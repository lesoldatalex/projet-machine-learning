Models folder

- `models/registry/` : local model registry (saved model artifacts)
- `models/production/` : staging/production-ready serialized models

When you log models to MLflow, you can copy the artifacts into `models/registry/` or use MLflow's model registry.